# openerp — 金蝶云·星空(K3 Cloud) 只读查询 CLI 任务报告

- **报告日期**：2026-06-02
- **对接实例**：`akeparking.ik3cloud.com`（用户已登录的真实生产实例）
- **总体结论**：核心查询能力 `ExecuteBillQuery` 已被验证为“换 FormId 即查”的万能且稳定接口，主数据/采购/销售/库存/计划/生产/委外七大主干领域全部返回真实生产数据，已达生产可用水平；少数对象因暂无数据、字段待发现、需专用接口或子系统未购买而受限，均已划清边界并列入后续专项，不影响通用查询能力的整体结论。

---

## 一、覆盖计分板

本阶段在真实实例上对 **42 个查询对象**逐一做了只读接口验证（全程经 `ExecuteBillQuery`/`QueryBusinessInfo`，每对象取 top1）。按状态统计如下：

| 状态 | 含义 | 数量 |
|---|---|---|
| ✅ 可查(有数据) | 接口可用且本实例有真实数据(附示例单号) | 34 |
| ⚪ 可查(空) | 接口可用、返回空数组(本实例暂无数据) | 4 |
| ⚠️ 待定字段 | 对象存在，需先用 `schema` 发现正确字段名 | 2 |
| 🔧 需专用接口 | 通用查询不适用，须接专用接口 | 1 |
| ⛔ 未购买 | 该子系统未购买(未授权) | 1 |
| **合计** | | **42** |

- **接口连通率**：40/42 ≈ **95.2%**（✅ + ⚪，即接口本身可用，仅个别因暂无数据返回空）。
- **生产可用率（已取到真实数据）**：34/42 ≈ **81.0%**。
- 去除 🔧 需专用接口与 ⛔ 未购买两类边界对象后，剩余 40 个列表型对象的 `ExecuteBillQuery` 通用查询适用率为 **100%**（其中 2 个仅需先经 `schema` 坐实字段、4 个暂无数据但接口连通）。

---

## 二、执行摘要与任务背景

### 2.1 目标

将金蝶云·星空(K3 Cloud)封装为一款**只读查询 CLI**(项目代号 `openerp`，基于 Go + Cobra)。核心约束为**只读**——全程仅调用查询类接口，绝不触碰任何写操作；能力诉求为**尽量覆盖多业务领域**，从主数据、采购、销售、库存，到生产、委外、计划、工程等模块，以单一工具贯通查询。

### 2.2 为什么做

在 AI agent 时代，ERP 数据需要以“机器可调用、可编排”的形态对外暴露。金蝶云原生的 WebAPI 虽然完备，但形态零散、参数繁琐，不利于 AI agent 直接消费。将其封装为 CLI，并辅以严格的 agent 契约（JSON Envelope 输出、强类型错误码、规范退出码、`stdout` 仅承载数据），即可让 AI agent 像调用本地命令一样安全、可预期地查询 ERP，这是把金蝶云接入智能体工作流的关键一步。

### 2.3 已完成工作

**POC 已完成并上线 GitHub**(`xiaowen-0725/openerp-cli`)，具备四项基础能力：鉴权(`LoginBySign` + session 复用 + 失效自动重登)、通用查询(`ExecuteBillQuery`)、领域命令(`bom`)、以及前述 agent 契约。

**本阶段**在 POC 之上，面向真实生产实例 `akeparking.ik3cloud.com` 逐模块开展只读接口测试：通过用户已登录的同源 Chrome 会话直接 `POST` 调用金蝶 WebAPI，全程只走 `ExecuteBillQuery`/`QueryBusinessInfo` 两类只读接口，对每个业务对象取 top1 验证其可用性与真实数据，覆盖 12 个业务模块、42 个查询对象。

---

## 三、接口能力与测试方法

### 3.1 四个只读接口的能力与测试状态

金蝶云·星空的 `ExecuteBillQuery` 通用列表查询接口具备“换 FormId 查任意单据/基础资料”的万能能力，配合 `QueryBusinessInfo`(字段元数据发现)、`View`(单据详情)、`BOS_ObjectType`(业务对象发现) 三个接口，可在不触碰任何写操作的前提下覆盖多业务领域的查询需求。

#### 3.1.1 ExecuteBillQuery —— 通用列表查询（可用·万能）

整套 CLI 的查询基石。其核心特性是**换 FormId 即可查询任意单据或基础资料**，无需为每类对象单独开发接口：

- 支持点号关联字段（如 `FSupplierId.FName` 直接取出供应商名称），免去二次查询；
- 支持 SQL 式过滤表达式，可按单号、状态、日期等条件筛选；
- 支持 `StartRow` 分页（单页上限约 2000 行），通过翻页获取大结果集。

**测试状态：可用，且已对多对象返回真实数据。** 本阶段以此接口为主力，对 8 大业务领域、共 40 余个对象逐一做了 top1 验证，绝大多数返回真实单据/资料数据（详见第四章）。

#### 3.1.2 QueryBusinessInfo —— 字段元数据发现（可用·协议已定）

用于发现某业务对象的字段结构，是 `schema` 命令的底层支撑，也是排查 `FIELD_BAD` 类问题的关键工具。

- **请求协议已坐实**：`parameters` 参数需编码为**单个 JSON 串**（而非数组），这是联调中明确下来的关键细节；
- **返回结构**：`Result.NeedReturnData.Entrys[].Fields[]`，逐字段给出 `Key`(字段标识)、中文名、数据类型、枚举值、关联对象等元信息。

**测试状态：可用，协议已定。** 让 CLI 在面对未知对象时能先“问字段、再查数据”，坐实字段名后再发起 `ExecuteBillQuery`。

#### 3.1.3 View —— 单据详情（可用）

按单据编号查询某张单据的完整结构，包含表头与全部分录明细，适用于查看单张单据全貌。

**测试状态：可用。** 在 POC 阶段已通过 `bom view` 命令完成联调验证。

#### 3.1.4 BOS_ObjectType —— 业务对象发现（可用）

本质是用 `ExecuteBillQuery` 去查询系统元对象表 `BOS_ObjectType`，是 `objects` 命令的支撑：

- 关键字段：`FID`(即对象的 FormId)、`FName`(对象名称)、`FSubSystemId`(所属子系统)；
- 可按关键字搜索系统中**全部业务对象**，用于运行期动态发现可查询的 FormId，而非依赖硬编码清单。

**测试状态：可用。** 可作为发现层(objects + schema)的入口，配合 `QueryBusinessInfo` 实现“先发现对象 → 再发现字段 → 最后查数据”的完整链路。

### 3.2 测试方法

本阶段采用**贴近真实使用场景的只读直调**方式：

1. **基于浏览器已登录会话直调 WebAPI**：通过用户已在 Chrome 中登录的真实生产实例会话（同源、携带 `kdservice-sessionid` cookie）直接 `POST` 调用金蝶 WebAPI。此方式复用既有登录态，无需额外鉴权配置，且权限边界与登录用户完全一致。
2. **每对象 top1 验证**：对每个业务对象用 `ExecuteBillQuery` 取第一行做存在性与字段可读性验证，快速确认“接口是否可用、该实例是否有数据、字段名是否正确”，而非拉取全量数据。
3. **全程只读，绝不写入**：整个测试过程仅调用 `ExecuteBillQuery` 与 `QueryBusinessInfo` 等只读接口，**绝不调用任何写接口**（新增、修改、删除、提交、审核等），从机制上保证对生产实例零副作用。

### 3.3 状态图例

| 状态 | 标识 | 含义 |
|---|---|---|
| `OK` | ✅ | 接口可用，且该实例有真实数据（附示例单号） |
| `EMPTY` | ⚪ | 接口可用，返回空数组（该模块此实例暂无数据） |
| `FIELD_BAD` | ⚠️ | 对象存在，但需用 `schema` 发现正确字段名后再查 |
| `SPECIAL` | 🔧 | 需专用接口（通用查询不适用） |
| `NOT_PURCHASED` | ⛔ | 该子系统未购买（未授权） |

### 3.4 关键约束

1. **查询建议带过滤条件**：为防止全表扫描导致超时或返回超大结果集，查询时应尽量携带过滤条件（按单号、日期、状态等限定范围）。
2. **单页上限约 2000 行，靠 `StartRow` 翻页**：单次查询返回行数上限约为 2000，超出部分需通过 `StartRow` 参数分页逐页获取。
3. **字段级 / 数据级权限自动生效**：接口自动套用登录用户的字段级与数据级权限，无权限的字段返回 `null`，无需 CLI 额外处理——这也是只读直调安全性的重要保障。
4. **报表/账表需走 GetSysReportData**：报表与账表类对象（模型类型 `900`、以 GUID 命名，如“销售订单执行明细表”）**不能**通过 `ExecuteBillQuery` 查询，须改用专用的 `GetSysReportData` 接口；亦可退而用对应单据的分录（如以销售出库 `SAL_OUTSTOCK` 分录）复现明细数据。

---

## 四、模块查询覆盖明细（核心）

本阶段以“发现层(objects/schema)打底、运行期逐对象覆盖”为思路，在真实实例上对 42 个查询对象逐一做了只读验证。覆盖结果按业务模块分组明细如下。

**状态图例**：✅ 接口可用且本实例有真实数据(附示例单号) ｜ ⚪ 接口可用但该实例暂无数据(返回空数组) ｜ ⚠️ 对象存在但需先用 schema 发现正确字段名 ｜ 🔧 需专用接口(不能走通用 ExecuteBillQuery) ｜ ⛔ 该子系统未购买(未授权)

| 业务模块 | 查询对象(中文) | FormId | 接口测试状态 | 证据/示例 |
|---|---|---|---|---|
| 主数据 | 物料 | BD_MATERIAL | ✅ 有数据 | 示例单号 `1.10.20.0001` |
| 主数据 | 客户 | BD_Customer | ✅ 有数据 | 示例单号 `01.01.002` |
| 主数据 | 供应商 | BD_Supplier | ✅ 有数据 | 示例单号 `01.001` |
| 主数据 | 部门 | BD_Department | ✅ 有数据 | 示例单号 `01` |
| 主数据 | 仓库 | BD_STOCK | ✅ 有数据 | 示例单号 `01.001` |
| 主数据 | 员工 | BD_Empinfo | ✅ 有数据 | 示例单号 `10001` |
| 采购管理 | 采购申请 | PUR_Requisition | ✅ 有数据 | 示例单号 `CGSQ000002` |
| 采购管理 | 采购订单 | PUR_PurchaseOrder | ✅ 有数据 | 示例单号 `ZPCG2020101653S` |
| 采购管理 | 采购入库 | STK_InStock | ✅ 有数据 | 示例单号 `CGRK00004` |
| 采购管理 | 采购退料 | PUR_MRB | ✅ 有数据 | 示例单号 `CGTL000003` |
| 采购管理 | 采购价目 | PUR_PriceCategory | ✅ 有数据 | 示例单号 `CGJM000001` |
| 销售管理 | 销售订单 | SAL_SaleOrder | ✅ 有数据 | 示例单号 `XSDD000006` |
| 销售管理 | 销售出库 | SAL_OUTSTOCK | ✅ 有数据 | 示例单号 `XSCKD000044` |
| 销售管理 | 发货通知 | SAL_DELIVERYNOTICE | ✅ 有数据 | 示例单号 `FHTZD000003` |
| 销售管理 | 销售退货 | SAL_RETURNSTOCK | ✅ 有数据 | 示例单号 `XSTHD000001` |
| 销售管理 | 销售报价 | SAL_QUOTATION | ✅ 有数据 | 示例单号 `XSBJD0001` |
| 销售管理 | 退货通知 | SAL_RETURNNOTICE | ✅ 有数据 | 示例单号 `THTZD000001` |
| 库存管理 | 其他入库 | STK_MISCELLANEOUS | ✅ 有数据 | 示例单号 `QTRK000001` |
| 库存管理 | 其他出库 | STK_MisDelivery | ✅ 有数据 | 示例单号 `QTCK000001` |
| 库存管理 | 直接调拨 | STK_TransferDirect | ✅ 有数据 | 示例单号 `ZJDB000001` |
| 库存管理 | 调拨申请 | STK_TransferApply | ✅ 有数据 | 示例单号 `DBSQ000002` |
| 库存管理 | 即时库存现量 | STK_InventoryQuery | 🔧 需专用接口 | QueryBusinessInfo 返回空模型，常规字段名报“字段不存在”，需专用库存查询接口 |
| 工程数据 | 物料清单 BOM | ENG_BOM | ✅ 有数据 | 示例单号 `1.20.01.0001 V3.0` |
| 工程数据 | 工艺路线 | ENG_Route | ⚪ 暂无数据 | 接口可用，返回空数组 |
| 工程数据 | 工作中心 | ENG_WorkCenter | ⚪ 暂无数据 | 接口可用，返回空数组 |
| 计划管理 | 计划订单 | PLN_PLANORDER | ✅ 有数据 | 示例单号 `MRP00000006` |
| 计划管理 | 预测单 | PLN_FORECAST | ✅ 有数据 | 示例单号 `FO00000008` |
| 生产管理 | 生产订单 | PRD_MO | ✅ 有数据 | 示例单号 `MO000005` |
| 生产管理 | 生产用料清单 | PRD_PPBOM | ✅ 有数据 | 示例单号 `PPBOM00000006` |
| 生产管理 | 生产领料 | PRD_PICKMTRL | ✅ 有数据 | 示例单号 `SOUT00000002` |
| 生产管理 | 生产汇报 | PRD_MORPT | ✅ 有数据 | 示例单号 `SCHB00000001` |
| 生产管理 | 生产入库 | PRD_INSTOCK | ✅ 有数据 | 示例单号 `SCRK00000001` |
| 生产管理 | 生产退料 | PRD_ReturnMtrl | ✅ 有数据 | 示例单号 `SCTL00000001` |
| 委外管理 | 委外订单 | SUB_SUBREQORDER | ✅ 有数据 | 示例单号 `SUB00000002` |
| 委外管理 | 委外用料清单 | SUB_PPBOM | ✅ 有数据 | 示例单号 `SUBBOM00000002` |
| 委外管理 | 委外领料 | SUB_PICKMTRL | ✅ 有数据 | 示例单号 `WWLL00000003` |
| 委外管理 | 委外退料 | SUB_ReturnMtrl | ✅ 有数据 | 示例单号 `WWTL00000001` |
| 车间管理 | 车间投入产出 | CB_SFCINOUTPUTBILL | ⚪ 暂无数据 | 接口可用，返回空数组 |
| 车间管理 | 车间成本计算 | CB_SFCCostCalBill | ⚠️ 需 schema 定字段 | 对象存在，但 FBillNo/FNumber 非其字段，需先 QueryBusinessInfo 发现正确字段名 |
| 车间管理 | 工序 | PLM_STD_PROCESS | ⚠️ 需 schema 定字段 | 对象存在，但 FBillNo/FNumber 非其字段，需先 QueryBusinessInfo 发现正确字段名 |
| 电商与分销 | 零售单 | CMK_LS_Retail | ⚪ 暂无数据 | 接口可用，返回空数组 |
| 成本管理 | 标准成本卷算 | CA_CostRollup | ⛔ 子系统未购买 | 返回“名称为标准成本分析的模块/子系统未购买”，属授权边界，非接口问题 |
| 成本管理 | 核算维度 | BD_FLEXITEMPROPERTY | ✅ 有数据 | 示例单号 `HSWD01_SYS` |

### 各模块小结

- **主数据**：6 个核心基础资料(物料/客户/供应商/部门/仓库/员工)全部贯通，均返回真实数据，可作为各业务单据关联字段(如 `FSupplierId.FName`)的解析基底。
- **采购管理**：申请-订单-入库-退料-价目全链路 5 个对象全部有数据，采购业务闭环可完整查询（注意采购入库走 `STK_InStock`）。
- **销售管理**：报价-订单-发货通知-出库-退货通知-退货 6 个对象全部有数据，覆盖正向销售与逆向退货完整链路。
- **库存管理**：其他出入库与调拨类 4 个对象均有数据；唯“即时库存现量”无法走通用查询，须列为专用库存接口专项。
- **工程数据**：BOM 已联调并返回真实数据(POC 重点)；工艺路线、工作中心接口可用但本实例暂无业务数据。
- **计划管理**：计划订单、预测单 2 个对象均有数据，MRP 计划链路可查。
- **生产管理**：生产订单及其下游用料清单-领料-汇报-入库-退料 7 个对象全部有数据，是本实例数据最完整的业务域之一。
- **委外管理**：委外订单-用料清单-领料-退料 4 个对象全部有数据，委外业务闭环可完整查询。
- **车间管理**：车间投入产出暂无数据；车间成本计算、工序两对象存在但默认字段名不适用，须先经 schema 坐实字段再纳入。
- **电商与分销**：零售单接口可用，但本实例暂无零售业务数据。
- **成本管理**：核算维度有数据；标准成本卷算受限于该实例未购买“标准成本分析”子系统，属授权边界而非技术问题。

---

## 五、异常、纠偏与局限

本章汇总本阶段逐模块只读实测中暴露的接口异常、对原有文档假设的纠偏，以及当前能力边界。以下结论均来自实测，未作任何推断或编造。

### 5.1 即时库存现量（STK_InventoryQuery）需专用库存接口——文档假设被推翻

原文档假设即时库存可走通用 `ExecuteBillQuery` 查询。实测中对该 FormId 调用 `QueryBusinessInfo` 进行字段发现时，返回的是空模型(`pk=null`)，且所有常规字段名(`FBillNo`/`FNumber` 等)均报“字段不存在”。这表明 `STK_InventoryQuery` 并非标准的单据/基础资料对象，无法通过通用列表查询接口取数。**结论**：即时库存现量必须改用金蝶专用的库存查询接口，原“走通用查询”的假设不成立，已据实纠偏。这是本阶段最重要的一处文档纠偏，为后续接口选型提供了确凿依据。

### 5.2 报表/账表（销售明细表等）需 GetSysReportData

所谓“销售明细表”在该实例中的真实对象为“销售订单执行明细表”，属报表/账表类对象(模型类型 900、采用 GUID 命名)。此类对象不在 `ExecuteBillQuery` 的能力范围内（见第三章关键约束）。取数方案有二：其一，改用报表专用接口 `GetSysReportData`；其二，退而用对应单据复现明细，例如基于销售出库单 `SAL_OUTSTOCK` 的分录数据自行汇总。本阶段未将其纳入通用查询，后续列为专项。

### 5.3 标准成本卷算（CA_CostRollup）所属子系统未购买——授权边界，非接口问题

对 `CA_CostRollup` 取数时，系统直接返回“名称为标准成本分析的模块/子系统未购买”。这是该生产实例的授权/许可边界问题，而非接口能力或字段问题：该实例未购买“标准成本分析”子系统，故相关对象在运行期不可用。此项需明确归因于子系统授权，CLI 侧无需也无法绕过，应在能力清单中标注授权依赖。

### 5.4 车间成本计算（CB_SFCCostCalBill）/工序（PLM_STD_PROCESS）需先用 schema 发现正确字段

这两个对象在系统中真实存在，但实测中以常规字段名 `FBillNo`/`FNumber` 查询时报错——这些并非其实际字段。原因是不同对象的主键/编号字段命名并不统一，不能套用通用字段名。**纠偏做法**：先调用 `schema`(即 `QueryBusinessInfo`)发现该对象的正确字段 `Key`，再据此构造 `ExecuteBillQuery` 查询。此类对象属“接口可用、但需 schema 定字段”，待字段坐实后即可纳入。

### 5.5 若干对象接口可用但该实例暂无业务数据

工艺路线(`ENG_Route`)、工作中心(`ENG_WorkCenter`)、车间投入产出(`CB_SFCINOUTPUTBILL`)、零售单(`CMK_LS_Retail`)四个对象，接口调用本身正常（FormId 有效、字段可识别），但 `ExecuteBillQuery` 返回空数组——说明该实例当前在这些模块尚无业务单据，属数据层面的“暂无数据”(EMPTY)，而非接口或权限缺陷。一旦实例中产生相应业务数据，现有查询即可直接取数，无需改动。

### 5.6 边界归类小结

上述情形对应三种不同性质的边界，须区别对待：

- **接口性质特殊（SPECIAL，🔧）**：即时库存现量、报表/账表——通用查询不适用，须接专用接口(`GetSysReportData`、库存接口)，列为专项。
- **字段待发现（FIELD_BAD，⚠️）**：车间成本计算、工序——接口可用，先以 schema 坐实字段即可纳入。
- **授权与数据边界**：标准成本卷算属子系统未购买(NOT_PURCHASED，⛔，授权边界)；工艺路线、工作中心、车间投入产出、零售单属暂无数据(EMPTY，⚪，数据边界)，二者均非接口缺陷。

---

## 六、落地建议与成功标准

### 6.1 总体策略：发现层打底 + 精选领域人性化

金蝶云·星空业务对象数量庞大、字段命名因实例配置而异，逐个对象硬编码命令既不现实也不可维护。因此落地采取“两层”策略：

- **发现层（objects + schema）打底，保证全覆盖。** 以两个已验证的通用接口为基础设施，做到运行期对任意业务对象可发现、可查询：
  - `openerp objects <关键字>` —— 基于 `BOS_ObjectType`(字段 `FID=FormId`、`FName`、`FSubSystemId`)，用 `ExecuteBillQuery` 搜索系统全部业务对象，回答“有什么对象、FormId 是多少”。
  - `openerp schema <FormId>` —— 基于 `QueryBusinessInfo`，返回每个字段的 `Key`/中文名/类型/枚举/关联对象，回答“这个对象有哪些字段、字段名叫什么”。
  - `openerp query <FormId> --fields ... --filter ...` —— 基于万能的 `ExecuteBillQuery`，支持点号关联字段、SQL 式过滤、`StartRow` 分页，是所有查询的执行底座。

  这三件套使 CLI 在不新增代码的前提下即可触达全部对象，并天然消化“对象存在但字段名不对(如 `CB_SFCCostCalBill`、`PLM_STD_PROCESS` 的 `FBillNo/FNumber` 非其字段)”这类问题——先 `schema` 坐实字段，再 `query`。

- **精选领域做人性化命令 + skill。** 对已用真实数据验证可用、且业务高频的领域，在发现层之上封装语义化子命令(如 `bom`、`po`、`so`、`stock` 等)，固化常用字段、关联展开、默认过滤与友好输出，并配套 skill 让 agent 直接调用。字段一律以 `schema` 实测结果为准，不沿用文档假设。

- **专项接口单独立项。** 两类对象不能走通用底座，明确隔离、专项实现：
  - 即时库存现量 `STK_InventoryQuery`：`QueryBusinessInfo` 返回空模型(`pk=null`)，常规字段名全部报“字段不存在”，需专用库存查询接口。
  - 报表/账表(模型类型 900、GUID 命名，如“销售订单执行明细表”)：不能走 `ExecuteBillQuery`，需 `GetSysReportData`；或退而用对应单据分录(如 `SAL_OUTSTOCK` 分录)复现明细。

- **尊重授权边界。** 字段级/数据级权限自动生效(无权字段返回 `null`)，子系统未购买会直接返回提示(如 `CA_CostRollup` 提示“标准成本分析子系统未购买”)。这类属授权/权限边界而非接口缺陷，CLI 应识别并以友好错误码透出，不做重试。

### 6.2 按模块分批落地顺序

落地顺序按“已验证可用 + 业务价值”双重排序，分四批推进。下列各模块状态均来自对实例的逐对象 top1 验证。

**第一批 · 主数据与核心交易（全部 OK，优先封装人性化命令）**

- 主数据：物料 `BD_MATERIAL`、客户 `BD_Customer`、供应商 `BD_Supplier`、部门 `BD_Department`、仓库 `BD_STOCK`、员工 `BD_Empinfo` —— 6 项全部 OK，是其余单据关联展开的基石，最先落地。
- 采购管理：采购申请 `PUR_Requisition`、采购订单 `PUR_PurchaseOrder`、采购入库 `STK_InStock`、采购退料 `PUR_MRB`、采购价目 `PUR_PriceCategory` —— 5 项全部 OK。
- 销售管理：销售订单 `SAL_SaleOrder`、销售出库 `SAL_OUTSTOCK`、发货通知 `SAL_DELIVERYNOTICE`、销售退货 `SAL_RETURNSTOCK`、销售报价 `SAL_QUOTATION`、退货通知 `SAL_RETURNNOTICE` —— 6 项全部 OK。

**第二批 · 库存、生产与委外（多数 OK）**

- 库存管理：其他入库 `STK_MISCELLANEOUS`、其他出库 `STK_MisDelivery`、直接调拨 `STK_TransferDirect`、调拨申请 `STK_TransferApply` —— 4 项 OK；即时库存现量 `STK_InventoryQuery` 标记为 SPECIAL，划入专项（见第四批）。
- 生产管理：生产订单 `PRD_MO`、生产用料清单 `PRD_PPBOM`、生产领料 `PRD_PICKMTRL`、生产汇报 `PRD_MORPT`、生产入库 `PRD_INSTOCK`、生产退料 `PRD_ReturnMtrl` —— 6 项全部 OK。
- 委外管理：委外订单 `SUB_SUBREQORDER`、委外用料清单 `SUB_PPBOM`、委外领料 `SUB_PICKMTRL`、委外退料 `SUB_ReturnMtrl` —— 4 项全部 OK。

**第三批 · 计划与工程（OK 项封装，EMPTY 项接口可用待数据）**

- 计划管理：计划订单 `PLN_PLANORDER`、预测单 `PLN_FORECAST` —— 2 项 OK。
- 工程数据：物料清单 BOM `ENG_BOM` 已在 POC 联调(OK)；工艺路线 `ENG_Route`、工作中心 `ENG_WorkCenter` 接口可用但该实例返回空数组(EMPTY)，命令可先实现，验收以接口连通为准、不强求数据。
- 成本管理：核算维度 `BD_FLEXITEMPROPERTY` OK，可纳入本批。

**第四批 · 待发现/专项/授权受限（先发现后纳入，不阻塞前三批）**

- 需先 `schema` 定字段后再纳入：车间成本计算 `CB_SFCCostCalBill`、工序 `PLM_STD_PROCESS`(FIELD_BAD，`FBillNo/FNumber` 非其字段)。
- 接口可用但当前无数据：车间投入产出 `CB_SFCINOUTPUTBILL`、零售单 `CMK_LS_Retail`(EMPTY)，随发现层自然覆盖。
- 专项接口：即时库存现量 `STK_InventoryQuery`(需专用库存接口)、报表账表(需 `GetSysReportData`)—— 独立立项排期。
- 授权受限：标准成本卷算 `CA_CostRollup`(标准成本分析子系统未购买，NOT_PURCHASED)—— 不实现，仅在错误透出层识别该提示。

### 6.3 可度量的成功标准

成功标准分四个维度，均可在 `akeparking.ik3cloud.com` 实例上客观验证。

**一、发现层（基础设施，硬性门槛）**

- `objects` 命令可按关键字搜索并返回系统业务对象的 `FormId`/`FName`/`FSubSystemId`，覆盖率目标 100%(凡 `BOS_ObjectType` 可查者皆可检出)。
- `schema` 命令对任意给定 `FormId` 返回完整字段元数据(`Key`/中文名/类型/枚举/关联对象)。
- `query` 命令支持点号关联字段、SQL 式过滤与 `StartRow` 分页，单页上限按 ~2000 处理并能自动翻页。
- 验收口径：对本报告所列全部 FORM 对象(不含 SPECIAL/NOT_PURCHASED)随机抽样，`schema → query` 链路均可走通。

**二、精选领域覆盖（人性化命令）**

- 第一至第三批中标记为 OK 的对象 100% 提供语义化子命令；以本次测试为基准，OK 对象不少于 30 个(主数据 6 + 采购 5 + 销售 6 + 库存 4 + 生产 6 + 委外 4 + 计划 2 + 工程 1 + 核算维度 1)。
- 每个人性化命令的字段集必须经 `schema` 实测坐实，不得引用未经验证的文档字段。
- 每个 OK 对象命令在实例上以其真实样例单号可回查(如 BOM `1.20.01.0001 V3.0`、采购订单 `ZPCG2020101653S`、销售订单 `XSDD000006`、生产订单 `MO000005`、委外订单 `SUB00000002` 等)，返回非空且字段语义正确。
- EMPTY 对象(工艺路线、工作中心、车间投入产出、零售单)以“接口连通、返回合法空数组”为通过标准。

**三、agent 可用性（契约门槛）**

- 严格遵循 POC 已建立的 agent 契约：输出 JSON Envelope、强类型错误、规范退出码、`stdout` 仅为数据。
- 错误可被 agent 机器识别并区分处理：字段不对(FIELD_BAD，引导调 `schema`)、对象需专用接口(SPECIAL)、子系统未购买(NOT_PURCHASED)必须映射为不同错误码，且对 NOT_PURCHASED 不做重试。
- 精选领域均配套 skill，agent 可在无人工干预下完成“发现对象 → 取字段 → 查数据”闭环。

**四、质量门禁（安全与稳定）**

- 全程只读：仅允许 `ExecuteBillQuery`/`QueryBusinessInfo`/`View`/`GetSysReportData` 等读接口，绝不调用任何写接口；以此作为合入红线。
- 查询默认带过滤或分页约束，防全表扫描/超时；单页上限 ~2000 由 CLI 兜底。
- 字段级/数据级权限边界透明：无权字段返回 `null` 属预期，不报错、不掩盖。
- 会话稳定：沿用 POC 的 `LoginBySign` + session 复用 + 失效自动重登机制，长时间批量查询不因会话过期中断。

---

## 七、下一步

1. **立即推进第一批人性化命令落地。** 主数据 6 项与采购/销售核心交易 11 项均已 OK，字段经 `schema` 坐实后封装语义化子命令并配套 skill，作为 agent 最先可用的高频能力。
2. **发现层(objects + schema + query)作为不变的基础设施先行固化。** 保证运行期对全部对象可发现、可查询，并把 FIELD_BAD 类对象(车间成本计算、工序)统一纳入“先 schema 后 query”的标准流程。
3. **将两个专用接口列为独立专项排期。** 即时库存现量(专用库存查询接口)与报表账表(`GetSysReportData`，或以对应单据分录复现)分别立项，不阻塞前三批通用领域的交付。
4. **在错误透出层明确授权边界。** 对标准成本卷算等未购买子系统的提示做强类型错误码映射，agent 侧识别 NOT_PURCHASED 后不重试，并在能力清单中标注授权依赖。
5. **EMPTY 对象随发现层自然覆盖。** 工艺路线、工作中心、车间投入产出、零售单命令可先实现，验收以“接口连通、返回合法空数组”为准；待实例产生业务数据后无需改动即可取数。
6. **持续以真实样例单号做回归校验。** 把本报告所列各 OK 对象的样例单号沉淀为回归用例，确保后续迭代中字段语义与可回查性不退化。
